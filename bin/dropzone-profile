#!/usr/bin/env node
var program = require('commander')

program
  .usage('<command> [options]')
  .description('Manage buyer and seller profiles')
  .command('buyer', 'Manage buyer profiles')
  .command('seller', 'Manage seller profiles')
  .parse(process.argv)
